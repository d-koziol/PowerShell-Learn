First, the script sets the domain name. Then, it retrieves the logged-in user’s username, creates a new credential object, and inserts the username into it.
Next, the script prompts the user to enter their password and adds it to the newly created credential.